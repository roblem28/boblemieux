# Populate this value with the connection string returned by
# `netlify add database curvegen`. The CLI typically provides a
# Netlify-managed Postgres connection string.
CURVEGEN_DATABASE_URL=postgres://USER:PASS@HOST:PORT/curvegen
